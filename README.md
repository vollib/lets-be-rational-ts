# lets-be-rational-ts

TypeScript port of Peter Jaeckel's LetsBeRational normalized Black and implied
volatility routines.

## Scripts

```sh
npm install
npm test
```

The current regression suite includes inherited JSON values and generated
C++ reference vectors. The shared language-neutral vector corpus lives in
`vollib/vollib-test-vectors`.
